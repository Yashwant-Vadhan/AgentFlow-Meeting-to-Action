# ── Core Framework ────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── Configuration & Utilities ─────────────────
python-dotenv>=1.0.0
pydantic-settings>=2.4.0
python-dateutil>=2.9.0

# ── Database ──────────────────────────────────
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# ── HTTP Client (for Ollama + n8n webhook) ────
httpx>=0.27.0

# ── WebSocket ─────────────────────────────────
websockets>=12.0

# ── Audio Processing (Vishal's modules) ───────
pydub>=0.25.1
noisereduce>=3.0.0
scipy>=1.13.0
numpy>=1.26.0
audioop-lts>=0.2.1  # Python 3.13+ compatibility (audioop removed from stdlib)

# ── Whisper (Yaashwanth's module) ─────────────
# faster-whisper>=1.0.0

# ── Testing ───────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-httpx>=0.30.0

# ── Logging ───────────────────────────────────
python-json-logger>=2.0.0
